# plotting the map with some points on it
ggmap(mapcolorado) +
  geom_point(data = df2, aes(x = long, y = latit, fill = "red", alpha = 0.8), size = 2, shape = 21) +
  guides(fill=FALSE, alpha=FALSE, size=FALSE)
