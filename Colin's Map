# plotting the map with some points on it
mapcolorado<-get_map(location="Denver")
ggmap(mapcolorado) + 
  geom_point(data = SpatialPoints(), aes(x = Larceny@coords[,1], y=Larceny@coords[,2], fill = "red", alpha = 0.8), size = 2, shape = 21) +
  guides(fill=FALSE, alpha=FALSE, size=FALSE)
